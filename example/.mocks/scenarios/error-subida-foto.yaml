name: Error 500 al subir foto
description: El POST de subida de foto falla con error interno
mocks:
  - method: POST
    url: '{{curation}}/api/products/12345/photos/'
    status: 500
    delay: 400
    response:
      errors:
        - internal error
