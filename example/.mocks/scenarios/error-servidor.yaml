name: Error del servidor al guardar
description: El POST falla con un error interno tras 400 ms
mocks:
  - method: POST
    url: '{{api}}/api/items/'
    status: 500
    delay: 400
    response:
      errors:
        - internal error
