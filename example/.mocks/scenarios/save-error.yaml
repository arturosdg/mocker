name: Save error
description: The POST fails with an internal error after 400 ms
mocks:
  - method: POST
    url: "{{api}}/api/items/"
    status: 500
    delay: 400
    response:
      errors:
        - internal error
